<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <title>¡Tu código de voucher!</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background: #f7f7fa;
        color: #222;
        margin: 0;
        padding: 0;
      }
      a {
        text-decoration: none !important;
      }
      .container {
        max-width: 480px;
        margin: 40px auto;
        background: #fff;
        border-radius: 12px;
        box-shadow: 0 2px 12px #0001;
        padding: 32px 24px;
      }
      .header {
        text-align: center;
        margin-bottom: 24px;
      }
      .voucher-code {
        font-size: 2rem;
        color: 98, 25, 212;
        font-weight: bold;
        letter-spacing: 2px;
        margin: 16px 0;
      }
      .cta {
        display: block;
        margin: 32px auto 0;
        background: #e67e22;
        color: #fff;
        padding: 12px 32px;
        border-radius: 6px;
        font-weight: 600;
        text-align: center;
      }
      .footer {
        margin-top: 32px;
        font-size: 0.9em;
        color: #888;
        text-align: center;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <img
          src="https://app.t-cert.us/sm-full-color.png"
          alt="Tcert Logo"
          style="height: 48px; margin-bottom: 12px"
        />
        <h2>
          ¡Felicidades! Has recibido un voucher para <% if (certificacion) { %>
          <span style="color: #e67e22"> <%= certificacion %></span>
          <% } %>
        </h2>
      </div>
      <p>Hola <%= nombre || 'estudiante' %>,</p>
      <p>Tu código de voucher es:</p>
      <div class="voucher-code"><%= codigo %></div>
      <p>
        Utilízalo en nuestra plataforma para acceder a tu curso certificador.
      </p>
      <a class="cta" href="https://app.t-cert.us/">Ir a la plataforma</a>
      <div class="footer">
        Si tienes dudas, contáctanos en
        <a href="mailto:tcert.ultra@gmail.com">tcert.ultra@gmail.com</a>.<br />
        © <%= new Date().getFullYear() %> T-cert
      </div>
    </div>
  </body>
</html>
